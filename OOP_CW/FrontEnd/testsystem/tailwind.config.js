export const content = [
  './index.html', // Look for classes in the index.html file
  './src/**/*.{js,ts,jsx,tsx}', // Look for classes in all JS, TS, JSX, TSX files inside src folder and its subdirectories
];
export const theme = {
  extend: {},
};
export const plugins = [];
